Create a deterministic simulation specification for: {{conceptName}}

## Concept Overview

{{conceptOverview}}

## Key Points

{{keyPoints}}

## Suggested Variables

{{variables}}

## Language

{{languageDirective}}

Model the concept with simple bounded numeric variables and linear time progression. At least one value must visibly change while the simulation runs. Choose meaningful units, ranges, defaults, rates and 0-4 useful presets.

Return only the minimal HTML envelope required by the system prompt. Put the complete JSON specification inside the single `structured-simulation-spec` application/json script tag. Do not generate runtime HTML controls, CSS, JavaScript, SVG, canvas code, external resources or explanations.